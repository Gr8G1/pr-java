package Gr8G1.prac.section.collection;

public class PrCollection {
  /*
   * # Collection
   *
   * Collection 인터페이스
   *  - 추가
   *    - boolean add(E e): 지정된 요소 추가
   *    - boolean addAll(Collection<? extends E> c): 전달된 컬렉션의 모든 요소를 추가
   *  - 삭제
   *    - void clear(): 컬렉션의 모든 요소 삭제
   *    - boolean remove(Object o): 전달된 요소를 컬렉션에서 삭제
   *    - boolean removeAll(Collection<?> c): 전달된 컬렉션의 모든 요소를 컬렉션에서 삭제
   *    - boolean retainAll(Collection<?> c): 전달된 컬렉션의 요소만을 남긴다. (나머지 삭제)
   *  - 검색
   *    - boolean contains(Object o): 전달된 요소가 존재하는지 확인
   *    - boolean containsAll(Collection<?> c): 전달된 컬렌션의 모든 요소가 존재하는지 확인
   *    - boolean equals(Object o): 전달된 요소와 컬렉션이 같은지 확인
   *    - int hashCode(): 컬렉션의 해시코드 반환
   *    - boolean isEmpty(): 컬렉션이 비어있는지 확인
   *    - Iterator<E> iterator(): 컬렉션의 반복자를 반환
   *    - int size(): 컬렉션의 크기를 반환
   *    - Object[] toArray(): 컬렉션의 모든 요소를 배열로 반환
   *    - <T> T[] toArray(T[] a): 이 컬렉션의 모든 요소를 포함하는 배열을 반환 (반환된 배열의 타입은 T 매핑)
   */
}

